- flash.each do |name, msg|
  - if msg.is_a?(String)
    %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", :type => "button"} &times;
    = content_tag :div, msg, :id => "flash_#{name}", :class => "alert alert-#{name} alert-dismissable"
